{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "omp-tandem": {
      "type": "stdio",
      "command": "uv",
      "args": ["run", "--no-project", "--python", ">=3.12", "python", "-I", "${PLUGIN_ROOT}/server.py"]
    }
  }
}
